import 'package:flutter/material.dart';

import 'login_screen.dart';

class Listview2Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Configuracion'),
        ),
        body: Text('Configuracion'));
  }
}
